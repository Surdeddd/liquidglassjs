---
'@surdeddd/liquidglass-core': minor
'@surdeddd/liquidglass-element': minor
---

Squircle shape: new `shape: 'squircle'` material param renders iOS-style superellipse surfaces across every backend — superellipse SDF in the displacement maps and the GL shader plus a polygon clip-path on styled hosts. The element gains a `shape` attribute.
